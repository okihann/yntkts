do GameState.set_player_can_move(false)
do GameState.set_ui_button_visibility(false)
There are enemies right there, kill them all and get your reward
do QuestBridge.quest_start("quest_dummy")
do GameState.set_player_can_move(true)
do GameState.set_ui_button_visibility(true)